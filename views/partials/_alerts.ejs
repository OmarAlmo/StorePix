
<% if(typeof errors != 'undefined'){ %>
<div>
    <% errors.forEach(function(error) { %>
        <%= error.msg %>
    <% }); %>
</div>
<% } %>



<% if(success_msg != ''){ %>
<div>
    <%= success_msg %>
    <span aria-hidden="true">&times;</span>
    </button>
</div>
<% } %>

<% if(error_msg != ''){ %>
<div>
    <%= error_msg %>
    <span aria-hidden="true">&times;</span>
    </button>
</div>
<% } %>



<% if(error != ''){ %>
<div>
    <%= error %>
    <span aria-hidden="true">&times;</span>
    </button>
</div>
<% } %>


